import React, { Fragment } from 'react'
import AccountCardShop from '../../Account-Card-Shop'

const Product = (props) => {
    return (
        <Fragment>
            <AccountCardShop {...props} />
        </Fragment>
    )
}

export default Product